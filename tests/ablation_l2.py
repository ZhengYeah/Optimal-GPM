from src.min_error_mechanism import MinL2MechanismAblation
from src.utilities import endpoints_to_lengths
from SW import SW_on_01

# piece_i = MinL2MechanismAblation(in_endpoint_a=0, in_endpoint_b=1, out_endpoint_a=-0.25608, out_endpoint_b=1.25608, epsilon=4, total_piece=3)
# p, l, val = piece_i.solve_probabilities()
# print(f"Probability List: {piece_i.probabilities}")
# l_list = piece_i.solve_lr(0.5)[0]
# error = piece_i.solve_lr(0.5)[1]
# print(f"Interval Lengths List: {l_list}")
# print(f"L1 error: {error}")

